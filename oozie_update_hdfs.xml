<?xml version="1.0" encoding="UTF-8"?>

<!-- Oozie Workflow for UPDATING all tables from RDBMS -->

<workflow-app xmlns="uri:oozie:workflow:0.4" name="rdbms-to-hdfs-casestudy-pt1-Workflow">

		<start to="sqoop_update_one"/>
		<!-- step 1 : Check first table-->	
		<action name="sqoop_update_one">
			<sqoop xmlns="uri:oozie:sqoop-action:0.2">
				<job-tracker>${jobTracker}</job-tracker>
				<name-node>${nameNode}</name-node>
				<prepare>					
					<!--
					<delete path="${nameNode}/user/maria_dev/hadoop_casestudy/sqoop_import1"/>	
					-->
				</prepare>
				<configuration>
					<property>
						<name>mapred.job.queue.name</name>
						<value>${queueName}</value>
					</property>
				</configuration>
				<command>job --meta-connect jdbc:hsqldb:hsql://localhost:16000/sqoop --exec sqp_update_table_one</command>		
			</sqoop>
			<ok to="create_temp_table_one"/>
			<error to="kill_job"/>
		</action>		
		<!-- step 2 : Check second table-->
		
		
	<end name="end"/>
</workflow-app>